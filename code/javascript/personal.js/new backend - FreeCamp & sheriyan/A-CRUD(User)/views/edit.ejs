<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Note taking app</title>
    <link rel="stylesheet" href="./stylesheets/index.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="main w-full min-h-screen bg-zinc-900 text-white p-4 md:p-10">
        <form action="/edit" method="post" class="grid grid-cols-1 md:grid-cols-2 gap-10">
            <!-- Old Information -->
            <div>
                <h2 class="text-xl mb-4">Old Information</h2>
                <div class="flex flex-col gap-4">
                    <input class="outline-none px-5 py-2 rounded-md bg-zinc-800" type="text" name="old-name" value="<%= user.name %>" readonly>
                    <input class="outline-none px-5 py-2 rounded-md bg-zinc-800" type="text" name="old-email" value="<%= user.email %>" readonly>
                    <input class="outline-none px-5 py-2 rounded-md bg-zinc-800" type="text" name="old-image" value="<%= user.image %>" readonly>
                </div>
            </div>
            <!-- New Information -->
            <div>
                <h2 class="text-xl mb-4">New Information</h2>
                <div class="flex flex-col gap-4">
                    <input class="outline-none px-5 py-2 rounded-md bg-zinc-800" required name="new-name" placeholder="New name">
                    <input class="outline-none px-5 py-2 rounded-md bg-zinc-800" required name="new-email" placeholder="New email">
                    <input class="outline-none px-5 py-2 rounded-md bg-zinc-800" required name="new-image" placeholder="New Profile image">
                    <span class="w-full flex gap-2 text-center">
                        <input class="bg-yellow-600 w-1/2 px-5 py-2 rounded-md cursor-pointer" type="submit" value="Update User">
                        <a class="bg-red-600 w-1/2  px-5 py-2 rounded-md cursor-pointer" href="/">Cancel</a>
                    </span>
                </div>
            </div>
        </form>
    </div>
</body>

</html>