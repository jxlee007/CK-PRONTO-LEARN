text = input()
word = input()

def search(x,y):
    x = str.split('')
    y = x.index()
    if len== len(y):
        print ("word is found")
    else:
        ("word not found")
        
print(search(text,word))